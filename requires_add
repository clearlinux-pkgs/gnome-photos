# This file contains additional runtime requirements that did not get
# picked up automatically. One name per line, no whitespace.

# requires schema 'org.freedesktop.Tracker.Miner.Files'
tracker-miners-data
